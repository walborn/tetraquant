# Система классификации компонентов

## 🎯 UI Components `components/ui`

Базовые строительные блоки из shadcn, не знающие о бизнес-логике:
- Переиспользуемые примитивы
- Компоненты без знания о домене приложения
- То, что можно вынести в отдельную библиотеку


```ts
ui/
├── button.tsx      // <Button variant="outline">
├── input.tsx       // <Input placeholder="Search...">
├── dialog.tsx      // <Dialog><DialogContent>...
├── table.tsx       // <Table><TableHeader>...
└── card.tsx        // <Card><CardTitle>...
```

## 🏗️ Layout Components `components/layout`
Компоненты, определяющие структуру страницы:
- Компоненты, которые wrap'ают контент
- То, что определяет общий вид приложения
- Компоненты с позиционированием

```ts
layout/
├── header.tsx      // Шапка с навигацией
├── sidebar.tsx     // Боковая панель
├── main-layout.tsx // Общий layout приложения
└── dashboard-layout.tsx // Специфичный layout
```

## 🔄 Shared Components `components/shared`
Бизнес-компоненты, переиспользуемые в разных частях приложения:
- Компоненты, знающие о домене приложения
- Составные компоненты из UI-примитивов
- То, что используется в нескольких страницах/разделах

```ts
shared/
├── user-avatar.tsx     // Аватар + имя пользователя
├── search-box.tsx      // Поиск с автодополнением
├── knowledge-card.tsx  // Карточка статьи базы знаний
├── profile-form.tsx    // Форма редактирования профиля
└── department-badge.tsx // Бейдж отдела
```

## 🚀 Feature Components `components/features`
!! Внимание !! Перед использованием очень сильно подумать

Когда проект растет, появляется необходимость выносить большие куски функционала в отдельные компоненты
Сюда попадает то, что нарушает принцип SOLID

```ts
features/
├── knowledge-base/
│   ├── article-editor.tsx
│   ├── article-list.tsx
│   └── category-selector.tsx
└── profile/
    ├── profile-details.tsx
    ├── security-settings.tsx
    └── activity-feed.tsx
```

## 📋 Проверочные вопросы
Перед тем, как выбрать папку сначала спроси себя:

1. Это базовый строительный блок? → `ui/`
2. Это определяет структуру страницы? → `layout/`
3. Это переиспользуется в разных разделах приложения? → `shared/`
4. Это относится к конкретной фиче и что-то сложное? → `features/`

### Пример из проекта:
```tsx
// ui/button.tsx - НЕ ЗНАЕТ о бизнесе
export const Button = ({ variant, children, ...props }) =>
  <button className={cn(variantStyles[variant])} {...props}>{children}</button>

// shared/user-avatar.tsx - ЗНАЕТ о пользователях
export const UserAvatar = ({ user, showName }) => (
  <div className="flex items-center gap-2">
    <Avatar>
      <AvatarImage src={user.avatar} />
      <AvatarFallback>{user.name[0]}</AvatarFallback>
    </Avatar>
    {showName && <span>{user.name}</span>}
  </div>
)

// layout/header.tsx - ОПРЕДЕЛЯЕТ структуру
export const Header = () => (
  <header className="border-b">
    <div className="flex h-16 items-center px-4">
      <AppLogo />
      <Navigation />
      <UserAvatar user={currentUser} showName />
    </div>
  </header>
)
```

## ! Замечание
Начинать нужно с трех категорий (`ui/`, `layout/`, `shared/`).
Когда в `shared/` станет больше 20 компонентов - можно выделить `features/` для группировки по доменным областям
