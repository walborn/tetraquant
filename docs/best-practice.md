# 🏆 Best Practices & Development Guidelines

Этот документ описывает основные принципы разработки, архитектурные подходы и стандарты качества принятые в проекте. Он дополняет, но не заменяет существующие гайды в папке `docs/`.

## 🏗 Архитектура и Принципы Кода (SOLID)

Мы стремимся писать код, который легко поддерживать и расширять. Для этого мы адаптируем принципы SOLID к React-разработке.

### 1. Single Responsibility Principle (SRP)
Каждый компонент или хук должен отвечать за одну вещь.
- **Bad**: Компонент `UserProfile` который сам фетчит данные, обрабатывает форму валидации и рендерит сложный UI.
- **Good**:
    - `useUserQuery` (Custom Hook) — получение данных.
    - `useUserForm` (Custom Hook) — логика формы.
    - `UserProfile` — презентационный компонент, получающий данные через пропсы или хуки.

### 2. Open/Closed Principle (OCP)
Программные сущности должны быть открыты для расширения, но закрыты для модификации.
- Используйте **Composition** (композицию) и `children` вместо добавления множества флагов (`isRed`, `isBig`, `withIcon`) внутрь компонента.
- Используйте **Slot Pattern** (передача компонентов через пропсы), чтобы менять содержимое без переписывания родителя.

### 3. Liskov Substitution Principle (LSP)
Наследники должны быть взаимозаменяемы. В контексте React это означает консистентность API пропсов.
- Если у вас есть `PrimaryButton` и `SecondaryButton`, они должны иметь одинаковый интерфейс (`onClick`, `disabled`, `className`).
- Не меняйте поведение стандартных пропсов (например, `onChange` должен возвращать событие или value единообразно во всем проекте).

### 4. Interface Segregation Principle (ISP)
Клиенты не должны зависеть от методов, которые они не используют.
- Не передавайте весь объект `user` (с 50 полями) в компонент `UserAvatar`, которому нужен только `avatarUrl`.
- Определяйте узкие интерфейсы пропсов: `type UserAvatarProps = { avatarUrl: string }`. Это делает компонент более переиспользуемым и легким для тестирования.

### 5. Dependency Inversion Principle (DIP)
Зависьте от абстракций, а не от деталей.
- Используйте **Context** или **Hooks** как слой абстракции для получения данных. Компонент не должен знать, откуда приходят данные (из REST API, GraphQL или LocalStorage).
- Это позволяет легко мокать данные в тестах и Storybook.

---

## 🗂 Архитектура Проекта

- Подробно структура описана в папке [Architecture](./architecture).
- **Co-location**: Держите тесты, стили и константы рядом с компонентом, к которому они относятся.
- **Feature-First**: Где возможно, группируйте код по фичам, а не по типу файлов.

---

## 🔀 Git Workflow & Процессы

### Названия Коммитов (Conventional Commits)
Мы следуем спецификации [Conventional Commits](https://www.conventionalcommits.org/). Это помогает автоматически генерировать changelog и понимать историю.

Формат: `type(scope): description`

- `feat`: Новая функциональность (feature).
    - `feat(auth): добавить вход через Google`
- `fix`: Исправление бага.
    - `fix(router): исправить редирект на 404`
- `refactor`: Изменение кода без изменения функциональности.
    - `refactor(ui): переписать Button на Tailwind`
- `docs`: Обновление документации.
- `chore`: Обновление сборки, конфигов, зависимостей.
- `test`: Добавление или исправление тестов.

### Работа с Ветками
- Основные правила описаны в [Gitflow](./gitflow.md).
- Название ветки должно отражать суть задачи: `feat/user-Profile`, `fix/login-error`.

### Code Review (Ревью Кода)
Нельзя мерджить в master напрямую, нужно мерджить сначала в dev.
Каждый Merge Request в dev должен пройти ревью перед вливанием.

**Для Автора:**
1.  **Self-Review**: Просмотрите свой diff сами перед тем как звать коллег. Удалите `console.log`, закомментированный код.
2.  **Описание**: Добавьте короткое описание, что делает MR.
3.  **Скриншоты/Видео**: ОБЯЗАТЕЛЬНО для UI задач "До/После".
4.  **Размер**: Старайтесь держать MR в пределах 400 строк кода. Лучше 3 маленьких MR, чем 1 огромный.

**Для Ревьюера:**
1.  Сначала ищите логические ошибки и архитектурные проблемы.
2.  Стиль кода (отступы, запятые) должен проверять `biome` (линтер), а не человек. Не тратьте время на споры о "вкусовщине", если линтер молчит.
3.  Если нашли ошибку линтера или хотите добавить новое правило, то делайте это в отдельном MR, прогнав его заново по всему коду.
4.  Будьте вежливы и конструктивны. Предлагайте решения.

---

## 🚀 Деплой и Инфраструктура

- Полный гайд по сборке и деплою находится в [Deploy Guide](../deploy.md).
- Мы используем **Bun** и **Docker**.
- Перед пушем всегда проверяйте, что проект собирается: `bun run build`. В CI настроены проверки, но лучше узнать об ошибке локально.

---

## 🛠 Особенности Стека

### Состояние (State)
- Ссылка на философию: [Philosophy](./phylosophy.md).
- **Server State** (Data Fetching): Используем **TanStack Query**. Никогда не пишите свои `useEffect` + `fetch` для загрузки данных.
- **URL State**: Состояние фильтров, пагинации, модалок храним в URL (TanStack Router Search Params). Это позволяет делиться ссылкой на "состояние" приложения.
- **Global Client State**: Используем React Context крайне осторожно, только для действительно глобальных вещей (Theme, Auth). Избегайте Redux/Zustand если задача решается первыми двумя способами.

### Стилизация
- Используем **Tailwind CSS**.
- Используйте утилиту `cn` (clsx + tailwind-merge) для условных классов.
- Избегайте `style={{ ... }}` атрибутов.
